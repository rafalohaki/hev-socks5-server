# Environment configuration for hev-socks5-server
# Copy this file to .env and customize the values

# ==================================================
# BASIC CONFIGURATION
# ==================================================

# SOCKS5 server port (default: 2137)
SOCKS5_PORT=2137

# Listen address (:: for all interfaces, 0.0.0.0 for IPv4 only)
LISTEN_ADDRESS=::

# Authentication (format: username:password)
# Leave empty to disable authentication (NOT recommended for production)
# Example: SOCKS5_AUTH=admin:your_secure_password_here
SOCKS5_AUTH=

# Timezone for logs and timestamps
TIMEZONE=Europe/Warsaw

# ==================================================
# ADVANCED CONFIGURATION
# ==================================================

# Docker container hostname
CONTAINER_HOSTNAME=socks5-proxy

# Resource limits
MEMORY_LIMIT=256M
CPU_LIMIT=1.0

# ==================================================
# SECURITY SETTINGS
# ==================================================

# Enable/disable specific Docker Compose profiles
# Available profiles: monitoring
COMPOSE_PROFILES=

# ==================================================
# EXAMPLE CONFIGURATIONS
# ==================================================

# Example 1: Basic setup with authentication
# SOCKS5_PORT=1080
# SOCKS5_AUTH=user:strongPassword123!
# LISTEN_ADDRESS=0.0.0.0

# Example 2: High-security setup
# SOCKS5_PORT=8080
# SOCKS5_AUTH=admin:$(openssl rand -base64 32)
# LISTEN_ADDRESS=127.0.0.1

# Example 3: Development setup (no auth)
# SOCKS5_PORT=2137
# SOCKS5_AUTH=
# LISTEN_ADDRESS=::

# ==================================================
# NOTES
# ==================================================

# 1. Always use strong passwords in production
# 2. Consider using port numbers other than default 1080
# 3. For production, bind to specific interfaces only
# 4. Remember to create logs directory: mkdir -p logs
# 5. Monitor logs regularly: docker compose logs -f

# Generate secure password command:
# openssl rand -base64 32